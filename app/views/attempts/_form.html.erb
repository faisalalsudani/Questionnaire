<%= form_for :exams do |f| %>

  <div id='answers'>
    <%= f.fields_for :exams do |exam| %>
      <%= exam %>
    <% end %>
  </div>

  <div class="actions">
    <%= f.submit "Finish exam", class: "btn btn-success" %>
  </div>
<% end %>
